# Go-Auth-API-With-CRUD
Here I've implemented a crud api where authentication also applies(so you have to first authenticate yourself via credentilas(email,password) before using any crud operations.
